@model TeacherViewModel

<div class="container cont-section" id="cont-modules-and-activities">
    <!--Modules and Activities-->
    <div class="row" >

        <div  class="col-sm-3 text-center">
            <div id="createajax"></div>

@if(User.IsInRole("Teacher"))
{
    <div>
 
    <h2 style="margin: 5px 25px 20px 0px">Modules<a  onclick="showInPopup('@Url.Action($"Create","Modules", new {id=Model.Current.course.Id},Context.Request.Scheme)','New Module')"   ><img style="margin: 0px 0px 5px 5px" id="btn-create" src="~/create.svg"  /></a></h2>
</div>
}
 

<partial name="ModuleList" model="@Model.ModuleList" />
        </div>
        <div  class="col-sm-9">
            <partial name="ActivityPartial" model="@Model.ActivityList" />
        </div>
    </div>
</div>


 